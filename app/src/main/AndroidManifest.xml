<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectoasesoriastecnm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoAsesoriasTecNM">
        <activity android:name=".AgendarActivity"></activity>
        <activity android:name=".MateriasActivity" />
        <activity android:name=".DashboardAsesoriaActivity" />
        <activity android:name=".DashboardMateriaActivity" />
        <activity android:name=".DashboardUsuarioActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".ProfesorMenuActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>